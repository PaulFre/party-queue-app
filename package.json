{
  "name": "party-queue-security-tests",
  "private": true,
  "scripts": {
    "test:firestore": "node --test test/firestore.rules.test.cjs",
    "test:firestore:emulator": "firebase emulators:exec --project demo-party-queue --only firestore \"npm run test:firestore\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.3",
    "firebase": "^10.14.1",
    "firebase-tools": "^14.24.0"
  }
}
